import 'package:flutter/material.dart';
import 'package:service_layer_spv/spv_service.dart';

class CalendarOverviewList extends StatelessWidget {

  final CompetitionOverviewBloc competitionOverviewBloc;

  const CalendarOverviewList({Key key, this.competitionOverviewBloc}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

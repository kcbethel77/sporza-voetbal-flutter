import 'package:flutter/material.dart';

class MatchEndSpacer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 25,
      color: Colors.grey[100],
    );
  }
}
